# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# Virtual environment
venv/
.venv/
env/

# Environment and secrets
.env
.env.local

# Data and artifacts (per lab instructions); track data/raw/*.dvc and data/raw/.gitignore in Git
# data/raw/dataset.csv — committed to repo for CI (Lab 4); full dataset in Git
data/processed/
data/prepared/
data/models/
models/
mlruns/

# Jupyter
.ipynb_checkpoints

# OS and misc
.trash/
*.log
.DS_Store

# Local DVC storage (not for remote repo)
dvc_storage/
